moveOnPath- moves it bit by bit as you call update

Physic Notes:
-on the grid, 
-- 0 means they don't collide/ can overlap
-- 1 means they do collide/ can't overlap
Set trigger fires an event

firebase login
firebase init
firebase deploy

Questions to ask:
-Figure out how targetable/targeting works

https://www.w3schools.com/tags/tryit.asp?filename=tryhtml5_canvas_arc
CanvasRenderer.ts where the drawing happens

run order:
Constructor
intiscene
loadscene
startscene